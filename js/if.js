//Enter two value find smallest.
// let a = 6 ; b= 4 ;
// if (a < b) {
//     console.log(" a is smallest  nymber")
//     } 
//     else {
//         console.log(" b is smallest number")
//         }

// Enter one value find its positive or negative.
// let c = 5 ; d = -10 ;
// if (c > 0) {
//     console.log(" c is positive number")
//     }
//     else {
//         console.log(" c is negative number")
        // }
        // if (c > d) {
        //     console.log(" c is positive number")
        //     }
        //     else {
        //         console.log(" c is negative number")
        //         }

// Enter one value find its divisible by 5 or not.
let e = 10 ; f = 12 ;
if (e % 5 == 0) {
    console.log(" e is divisible by 5")
    }
    else {
        console.log(" e is not divisible by 5")
        }
// if (f % 5 == 0) {
//             console.log(" f is divisible by 5")
//         }
//     else {
//             console.log(" f is not divisible by 5")
//         }



//  Enter one value find its divisible by 5 and 3 or not
let g = 15 ; h = 18 ;
if (g % 5 == 0 && g % 3 == 0) {
    console.log(" g is divisible by 5 and 3")
    }
    else {
        console.log(" g is not divisible by 5 and 3")
        }
        // if (h % 5 == 0 && h % 3 == 0) {
        //     console.log(" h is divisible by 5 and 3")
        // }
        // else {
        //     console.log(" h is not divisible by 5 and 3")
        // }


// Enter one value find its divisible by 5 or 3 or both
    let i = 15 ; j = 18 ;
        if (i % 5 == 0 || i % 3 == 0) {
            console.log(" i is divisible by 5 or 3 or both")
            }
        else {
                console.log(" i is not divisible by 5 or 3 or both")
                }
         // if (j % 5 == 0 || j % 3 == 0)
                // {
                //     console.log(" j is divisible by 5 or 3 or both")
                // }
                // else {
                //     console.log(" j is not divisible by 5 or 3 or both")
                // }
//Enter Students marks wheather pass or not.
let marks = 20 ;
if (marks >= 40) {
    console.log(" Student is pass ")
    }
    else {
        console.log(" Student is fail ")
        }

//  Enter year find its leap year or not.
let year = 2024 ;
if (year % 4 == 0 ) {
        console.log(" year is leap year ")
        }
        else {
            console.log(" year is not leap year ")
            }

// Enter one value find its odd or even
            let num = 15 ;
            if (num % 2 == 0) {
                console.log(" number is even ")
                }
                else {
                    console.log(" number is odd ")
                    }

// Enter a, b, c find largest among them.
let a = 10 ; 
let b = 20 ;
let c = 30 ;
if (a > b && a > c) {
    console.log(" a is largest ")
    }
    else if (b > a && b > c) {
        console.log(" b is largest ")
        }
        else {
            console.log(" c is largest ")
            }

// Enter k, l, m find smallest among them.
let k = 10 ;
let l = 20 ;
let m = 30 ;
if (k < l && k < m) {
    console.log(" k is smallest ")
    }
    else if (l < k && l < m) {
        console.log(" l is smallest ")
        }
        else {
            console.log(" m is smallest ")
            }
            
    

        