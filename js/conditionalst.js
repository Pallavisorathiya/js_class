/* wap program in javascript An electricity board charges the following rates to domestic users to
discourage large consumption of energy For the first 100 units 60P per unit
For the next 200 units 80P per unit Beyond 300 units 90P per unit All users
are charged a minimum of Rs. 50.If the total amount is more than 300.00
then an additional surcharge of 15 % is added.*/

                


 // wap program in javascript to check whether a person is eligible for voting or not using conditional statements?
//  let age = prompt(" enter your age") ;
//  let isEligible = (age >= 18) ? "Eligible" : "Not Eligible";
//  console.log(isEligible);

/*Write a javascript using nested loop program to accept the basic salary of an employee from the user.
Calculate the gross salary on the following basis 

 Basic            HRA      DA

 
 Up to 5000       8%       20%


 5000 to 10000    12%      30%


 10000 to 15000   15 %     40%


 15000 above      20%      50%  */


 
//  let basic =Number(prompt("enter salary"));
//  let hra = 0;
//   let da = 0;
//   let gross = 0;
//   if (basic <= 5000) {
//       hra = basic * 0.08;
//       da = basic * 0.20;
//   } else if (basic >= 5000 && basic < 10000) {
//      hra = basic * 0.12;
//       da = basic * 0.30;
//   } else if (basic >= 10000 && basic < 15000) {
//      hra = basic * 0.15;
//      da = basic * 0.40;
//   } else if (basic >= 15000) {
//       hra = basic * 0.20;
//       da = basic * 0.50;
//   }
//    gross = basic + hra + da;
//    console.log("Gross Salary is " + gross);

/* Write a program   in javascript to evaluate the Total, Percentage, Grade of a student for the
following constraints:*/
//  let per = 90;
// let per = prompt("enter percentage");
// if(per > 75){
//     console.log("you passed with A grade")
// }else if(per > 60 && per < 75){
//     console.log("you passed with B grade")
// }else if(per > 45 && per < 60){
//     console.log("you passed with c grade")
// }else if(per > 35 && per < 45){
//     console.log("you passed with d grade")
// }else if(per > 35){
//     console.log("you failed")
// }else{
//     console.log("Better luck next time")
// }
/* wap in javascript using nesteed loop if else if  Declare two variables x and y. Assign values to these variables. Number x should be printed only if it is less than 2000 or greater than 3000, and number y
should be printed only if it is between 100 and 500. */
let x = 2500;
let y = 600;
if(x < 2000 || x > 3000){
    console.log("x is " + x);
    }else{
        console.log("x is not between 2000 and 3000");
        }
        if(y > 100 && y < 500){
            console.log("y is " + y);
            }else{
                console.log("y is not between 100 and 500");
                }

     
















